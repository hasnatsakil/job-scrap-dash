version: '3.8'
services:
  app:
    build: .
    ports:
      - "8000:8000"
    environment:
      - PORT=8000
      - GOOGLE_SHEETS_CREDENTIALS_B64=
      - GOOGLE_SHEET_ID=
      - OPENROUTER_API_KEY=
      - LINKEDIN_LI_AT_COOKIE=
    volumes:
      - ./backend:/app/backend

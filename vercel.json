{
  "builds": [
    {
      "src": "frontend/package.json",
      "use": "@vercel/vite",
      "config": {
         "distDir": "dist"
      }
    }
  ],
  "rewrites": [
    { "source": "/test-rewrite", "destination": "/index.html" }
  ]
}
